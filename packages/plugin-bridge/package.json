{
  "name": "@stellar-agent-kit/plugin-bridge",
  "version": "0.1.10",
  "description": "Cross-chain bridging actions via Allbridge Core. List supported tokens, quote a transfer, and build a raw bridge transaction (consumer signs/submits).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --dts --clean --format esm --out-dir dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "peerDependencies": {
    "@stellar-agent-kit/core": "workspace:*",
    "@allbridge/bridge-core-sdk": "*"
  },
  "peerDependenciesMeta": {
    "@allbridge/bridge-core-sdk": {
      "optional": true
    }
  },
  "devDependencies": {
    "@stellar-agent-kit/core": "workspace:*",
    "@stellar/stellar-sdk": "^14.0.0-rc.3",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "publishConfig": {
    "access": "public"
  }
}
